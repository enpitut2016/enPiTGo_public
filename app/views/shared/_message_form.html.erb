<%= form_for @message do |f| %>
  <div class="form-group">
    <%= f.label "お困りごと内容" %>
    <%= f.text_field :subscription, size: 32, class:'form-control' %>
  </div>
  <%= f.submit "登録", :class => 'btn btn-primary btn-block' %>
<% end %>
